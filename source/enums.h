// A collection of enums that describe certain preference options.

typedef enum {
  Anchor_Left,
  Anchor_Center,
  Anchor_Right,
} AnchorPosition;

typedef enum {
  Round_Down,
  Round_Nearest,
  Round_Up,
} RoundingStyle;

typedef enum {
  Mode_Dots,
  Mode_Bar,
  Mode_Circle,
  Mode_Outline,
} Mode;

typedef enum {
  Orientation_Vertical,
  Orientation_Horizontal,
} Orientation;
